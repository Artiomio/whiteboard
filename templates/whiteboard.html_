<!doctype html>
<html>
    <head>
    <meta charset="utf-8">
    <body>
	<img id="im" width="800" height="600">
    	<script>
            function getUrl() { return "./screenshot?" + Math.random() }    	
            setInterval(function() {if (!document.hidden) document.getElementById("im").src = getUrl()}, 300);
    	</script>
    </body>
</html>